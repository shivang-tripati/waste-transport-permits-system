import { Stack } from 'expo-router';

export default function PermitsLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
